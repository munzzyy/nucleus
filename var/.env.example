# Nucleus optional secrets. Copy to var/.env (gitignored) and fill in.
# Everything works without these — the modules that need a key skip gracefully.

# Recon → live phone lookup (NumLookupAPI). Without it, phone falls back to
# parsed country/format + pivot links.
NUMLOOKUP_API_KEY=
